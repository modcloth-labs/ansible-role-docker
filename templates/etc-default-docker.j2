# vim:filetype=sh
# {{ ansible_managed }}
DOCKER_OPTS=""
DOCKER_OPTS="$DOCKER_OPTS -g {{ docker.graph_storage_dir }}"
{% for addr in docker.listen_addresses %}
DOCKER_OPTS="$DOCKER_OPTS -H {{ addr }}"
{% endfor %}
{% if docker.debug %}
DOCKER_OPTS="$DOCKER_OPTS -D"
{% endif %}
export DOCKER_OPTS
